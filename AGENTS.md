# 简派：AI 开发协作约定

## 每次启动 / 接续开发

- 先完整阅读 `TODO.md`，查看已完成、待办和验证状态；它是持续交接记录，不再维护 `HANDOFF.md`。
- 用户有明确需求时优先处理该需求；否则从待办中选择当前可以推进的事项，简短说明后开始。
- 没有待办，或剩余事项都需要用户确认 / 外部条件时，询问用户有什么需要实现的，不自行扩展需求。
- 完成的事项在 `TODO.md` 打勾；代码完成与构建、测试、实际体验验证分开记录，未验证不得打勾。
- 当前会话若运行于简派 `build/pi/`，不要执行 `npm ci` 或重建正在使用的目录。可做内存编译和源码测试；将完整构建验证列入待办，让用户退出后在普通终端执行。核心汉化更新必须重启，`/reload` 不会加载新核心代码。
- 不自动提交、发布或备份；不得用 `git clean` 清理未跟踪源码。临时验证使用独立配置，不使用用户真实密钥和会话。

## 产品原则

简派是面向非专业开发者的中文 Pi。读 README.md、docs/product.md、docs/ux-writing.md 和 docs/architecture.md 后再开始功能开发。

## 与用户沟通

- 默认使用简体中文，说明操作、影响和结果。
- 用户只有基本代码与终端知识；必要术语给一句通俗解释。
- 重要操作前简短说明目的，不逐条复述机械动作。
- 区分“打算做”“已执行”“已验证”，不把写完代码说成产品可用。
- 涉及删除、覆盖用户内容、发布、付费和全局配置变更时先说明并确认。

## 工程约定

- 第一阶段先汉化 Pi，保持原有功能和操作，不主动加入解释卡片、教学模式或风险拦截。用户已要求给 `ls`、`rg` 增加简短括号说明，仅限显示，不修改真实命令。
- 当前采用固定 Pi 0.85.0 发布包的独立显示补丁构建；维护 locales/，不要直接修改 node_modules/、build/ 或全局 Pi。
- 优先复用 Pi；接口必须以对应版本的官方文档与示例为依据。
- 实施扩展、TUI、SDK 等功能前完整阅读相关文档及引用。
- 中文呈现与工具执行分离，保留原始输入、输出、错误及代码差异。
- 不翻译代码标识符、命令、路径或协议字段。
- 不编造错误原因，不通过隐藏日志掩盖失败。
- 提示词不是强制权限控制，命令关键词匹配不是安全沙箱。
- 新增依赖说明用途；不得提交密钥、会话记录和个人配置。
- 实现功能时同步增加测试与使用说明，维护 TODO.md。

## 当前可用命令

- `npm ci --ignore-scripts`：安装锁定依赖，不运行依赖安装脚本。
- `npm run build`：生成独立中文构建。
- `npm run check`：检查语法、指纹、构建同步和执行层保持原样。
- `npm test`：本地测试，不调用付费模型。
- `npm start`：启动简派，默认使用独立的 ~/.jianpai/agent/ 配置。
- `npm run demo`：在临时目录实际运行四个工具的中文展示。
- `npm run audit:zh`：生成英文片段人工复核清单。

更改代码后运行构建、检查与测试；正在简派中开发时遵守上面的重建限制，将尚未执行的验证明确留在 `TODO.md`。新增补丁文件或升级上游前审查接口和文案，只有审核后才能用 `node scripts/build.mjs --record-upstream` 更新指纹。不要把本地工具测试说成真实服务商端到端验证。
